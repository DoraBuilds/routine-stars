import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // TODO: replace with your final store bundle id before publishing.
  appId: 'com.routinestars.app',
  appName: 'Routine Stars',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;

